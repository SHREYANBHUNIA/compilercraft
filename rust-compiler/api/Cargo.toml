[package]
name = "craft-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
craft-compiler = { path = "../compiler" }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }
